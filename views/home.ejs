<%- include("partials/header"); -%>
 
    <h1>Home</h1>
    <p> <%= homeStartingContent %> </p>

    <div>
        <% posts.forEach(function (post) { %>
        <div>
            <h1><%= post.title %></h1>
            <p> <%= post.content.substring(1, 100) + "..." %><a href="/posts/<%= post.title %>"> Read More</a></p>

        </div>
        <% }); %>
    </div>


<%- include("partials/footer"); -%>